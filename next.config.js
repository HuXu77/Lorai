/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // This tells Next.js to look for the app in src/app
    // (we're keeping everything in the src/ directory for consistency)
}

module.exports = nextConfig
