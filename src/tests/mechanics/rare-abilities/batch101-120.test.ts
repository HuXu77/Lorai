import { describe, it, expect, beforeEach } from 'vitest';
import { TestHarness } from '../../engine-test-utils';

// Batches 101-120: Final maximum coverage push - 120 more abilities
describe('DISARMING BEAUTY', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Aurora - Briar Rose']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Aurora - Briar Rose', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Aurora - Briar Rose']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('I WILL BE WITH YOU', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Gramma Tala - Storyteller']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Gramma Tala - Storyteller', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Gramma Tala - Storyteller']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('CARETAKER', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Jasmine - Queen of Agrabah']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Jasmine - Queen of Agrabah', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Jasmine - Queen of Agrabah']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('GIVE IT A TRY', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Maurice - World-Famous Inventor']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Maurice - World-Famous Inventor', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Maurice - World-Famous Inventor']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('IT WORKS!', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Maurice - World-Famous Inventor']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Maurice - World-Famous Inventor', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Maurice - World-Famous Inventor']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('INSIDIOUS PLOT', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Scar - Mastermind']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Scar - Mastermind', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Scar - Mastermind']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('WHAT HAVE WE HERE?', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Tamatoa - So Shiny!']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Tamatoa - So Shiny!', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Tamatoa - So Shiny!']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('GLAM', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Tamatoa - So Shiny!']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Tamatoa - So Shiny!', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Tamatoa - So Shiny!']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('BREAK', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Beast - Hardheaded']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Beast - Hardheaded', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Beast - Hardheaded']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('STOLEN DUST', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Captain Hook - Thinking a Happy Thought']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Captain Hook - Thinking a Happy Thought', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Captain Hook - Thinking a Happy Thought']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('UNDER ARREST', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Gantu - Galactic Federation Captain']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Gantu - Galactic Federation Captain', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Gantu - Galactic Federation Captain']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('STAGE A LITTLE ACCIDENT', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Hans - Thirteenth in Line']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Hans - Thirteenth in Line', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Hans - Thirteenth in Line']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('GUESS WHAT?', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Simba - Future King']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Simba - Future King', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Simba - Future King']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('I KNOW WHAT I HAVE TO DO', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Simba - Rightful Heir']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Simba - Rightful Heir', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Simba - Rightful Heir']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('AYE AYE, CAPTAIN', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Starkey - Hook\'s Henchman']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Starkey - Hook\'s Henchman', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Starkey - Hook\'s Henchman']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('SEEK THE HEART', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Te Kā - Heartless']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Te Kā - Heartless', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Te Kā - Heartless']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('OH, GOSH!', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Bashful - Hopeless Romantic']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Bashful - Hopeless Romantic', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Bashful - Hopeless Romantic']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('WE\'LL ALWAYS BE TOGETHER', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Christopher Robin - Adventurer']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Christopher Robin - Adventurer', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Christopher Robin - Adventurer']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('SHARE AND SHARE ALIKE', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Doc - Leader of the Seven Dwarfs']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Doc - Leader of the Seven Dwarfs', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Doc - Leader of the Seven Dwarfs']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('ODD ONE OUT', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Dopey - Always Playful']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Dopey - Always Playful', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Dopey - Always Playful']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('YES, YOUR MAJESTY', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Grand Duke - Advisor to the King']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Grand Duke - Advisor to the King', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Grand Duke - Advisor to the King']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('THERE\'S TROUBLE A-BREWIN\'', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Grumpy - Bad-Tempered']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Grumpy - Bad-Tempered', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Grumpy - Bad-Tempered']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('LAY IT ON THE LINE', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['King Louie - Jungle VIP']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['King Louie - Jungle VIP', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['King Louie - Jungle VIP']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('THE SUN WILL SET', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Mufasa - Betrayed Leader']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Mufasa - Betrayed Leader', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Mufasa - Betrayed Leader']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('HONOR TO THE ANCESTORS', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Mulan - Reflecting']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Mulan - Reflecting', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Mulan - Reflecting']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('NURSEMAID', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Nana - Darling Family Pet']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Nana - Darling Family Pet', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Nana - Darling Family Pet']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('YAWN!', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Sleepy - Nodding Off']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Sleepy - Nodding Off', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Sleepy - Nodding Off']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('AH-CHOO!', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Sneezy - Very Allergic']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Sneezy - Very Allergic', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Sneezy - Very Allergic']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('I WON\'T HURT YOU', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Snow White - Lost in the Forest']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Snow White - Lost in the Forest', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Snow White - Lost in the Forest']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('HOW DO YOU DO?', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Snow White - Unexpected Houseguest']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Snow White - Unexpected Houseguest', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Snow White - Unexpected Houseguest']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('WHO IS THE FAIREST?', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['The Queen - Commanding Presence']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['The Queen - Commanding Presence', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['The Queen - Commanding Presence']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('ETHEREAL GLOW', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Blue Fairy - Rewarding Good Deeds']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Blue Fairy - Rewarding Good Deeds', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Blue Fairy - Rewarding Good Deeds']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('FORGET THE COACH, HERE\'S A SWORD', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Fairy Godmother - Mystic Armorer']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Fairy Godmother - Mystic Armorer', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Fairy Godmother - Mystic Armorer']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('JUST LEAVE IT TO ME', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Fairy Godmother - Pure Heart']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Fairy Godmother - Pure Heart', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Fairy Godmother - Pure Heart']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('HE\'S BACK!', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['HeiHei - Persistent Presence']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['HeiHei - Persistent Presence', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['HeiHei - Persistent Presence']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('THAT STILL, SMALL VOICE', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Jiminy Cricket - Pinocchio\'s Conscience']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Jiminy Cricket - Pinocchio\'s Conscience', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Jiminy Cricket - Pinocchio\'s Conscience']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('CHASING THE RABBIT', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Madam Mim - Fox']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Madam Mim - Fox', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Madam Mim - Fox']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('GRUESOME AND GRIM', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Madam Mim - Rival of Merlin']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Madam Mim - Rival of Merlin', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Madam Mim - Rival of Merlin']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('JUST YOU WAIT', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Madam Mim - Snake']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Madam Mim - Snake', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Madam Mim - Snake']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('READY OR NOT!', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Merlin - Crab']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Merlin - Crab', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Merlin - Crab']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
