import { describe, it, expect, beforeEach } from 'vitest';
import { TestHarness } from '../../engine-test-utils';

// Batches 31-35: 30 abilities - ultra-compact
describe('A DELICATE BALANCE', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Mufasa - Ruler of Pride Rock']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Mufasa - Ruler of Pride Rock', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Mufasa - Ruler of Pride Rock']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('EVERYTHING THE LIGHT TOUCHES', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Mufasa - Ruler of Pride Rock']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Mufasa - Ruler of Pride Rock', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Mufasa - Ruler of Pride Rock']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('CHANGING THE CODE', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['King Candy - Sweet Abomination']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['King Candy - Sweet Abomination', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['King Candy - Sweet Abomination']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('HEAVE HO!', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Stitch - Team Underdog']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Stitch - Team Underdog', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Stitch - Team Underdog']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('NATURAL LEADER', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Snow White - Fair-Hearted']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Snow White - Fair-Hearted', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Snow White - Fair-Hearted']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('KNIGHTED BY THE KING', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Arthur - King Victorious']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Arthur - King Victorious', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Arthur - King Victorious']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('NO PLACE I\'D RATHER BE', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Lilo - Escape Artist']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Lilo - Escape Artist', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Lilo - Escape Artist']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('SEARCH AND RESCUE', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Chip \'n\' Dale - Recovery Rangers']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Chip \'n\' Dale - Recovery Rangers', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Chip \'n\' Dale - Recovery Rangers']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('SPECIAL RESERVATION', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Tiana - Restaurant Owner']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Tiana - Restaurant Owner', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Tiana - Restaurant Owner']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('ASTRAL ATTUNEMENT', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Rafiki - Ethereal Guide']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Rafiki - Ethereal Guide', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Rafiki - Ethereal Guide']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('FEEL THE POWER', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Yzma - Conniving Chemist']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Yzma - Conniving Chemist', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Yzma - Conniving Chemist']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('BACK, FOOLS!', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Maleficent\'s Staff']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Maleficent\'s Staff']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(1); }); it('r', async () => { await t.setPlay(t.p1Id, ['Maleficent\'s Staff']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('READY FOR ACTION', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Stitch - Alien Buccaneer']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Stitch - Alien Buccaneer', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Stitch - Alien Buccaneer']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('HERE\'S THE BEST PART', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Honey Lemon - Chemical Genius']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Honey Lemon - Chemical Genius', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Honey Lemon - Chemical Genius']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('CLEVER DESIGN', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Goofy - Expert Shipwright']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Goofy - Expert Shipwright', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Goofy - Expert Shipwright']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('TWISTS AND TURNS', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Basil - Disguised Detective']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Basil - Disguised Detective', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Basil - Disguised Detective']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('COME ON, LET\'S DO THIS', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Raya - Kumandran Rider']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Raya - Kumandran Rider', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Raya - Kumandran Rider']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('REMEMBER YOUR TRAINING', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Aladdin - Intrepid Commander']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Aladdin - Intrepid Commander', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Aladdin - Intrepid Commander']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('SWASH YOUR BUCKLES', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Tigger - In the Crow\'s Nest']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Tigger - In the Crow\'s Nest', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Tigger - In the Crow\'s Nest']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('TEETH AND AMBITIONS', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Scar - Heartless Hunter']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Scar - Heartless Hunter', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Scar - Heartless Hunter']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('MECHANICALLY SAVVY', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Gadget Hackwrench - Brilliant Bosun']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Gadget Hackwrench - Brilliant Bosun', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Gadget Hackwrench - Brilliant Bosun']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('BLADES OF FURY', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Wasabi - Methodical Engineer']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Wasabi - Methodical Engineer', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Wasabi - Methodical Engineer']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('FULLY CHARGED', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Baymax - Personal Healthcare Companion']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Baymax - Personal Healthcare Companion', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Baymax - Personal Healthcare Companion']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('YOU SAID \'OW\'', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Baymax - Personal Healthcare Companion']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Baymax - Personal Healthcare Companion', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Baymax - Personal Healthcare Companion']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('THE TREATMENT IS WORKING', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Baymax - Armored Companion']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Baymax - Armored Companion', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Baymax - Armored Companion']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('I WRECK THINGS', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Wreck-It Ralph - Ham Hands']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Wreck-It Ralph - Ham Hands', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Wreck-It Ralph - Ham Hands']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('DRIVELING GALOOTS', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Captain Amelia - Commander of the Legacy']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Captain Amelia - Commander of the Legacy', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Captain Amelia - Commander of the Legacy']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('EVERYTHING SHIPSHAPE', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Captain Amelia - Commander of the Legacy']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Captain Amelia - Commander of the Legacy', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Captain Amelia - Commander of the Legacy']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('WHO\'S NEXT?', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Perdita - Playful Mother']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Perdita - Playful Mother', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Perdita - Playful Mother']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('DON\'T BE AFRAID', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Perdita - Playful Mother']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Perdita - Playful Mother', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Perdita - Playful Mother']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('SWEET DREAMS', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Aurora - Waking Beauty']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Aurora - Waking Beauty', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Aurora - Waking Beauty']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
