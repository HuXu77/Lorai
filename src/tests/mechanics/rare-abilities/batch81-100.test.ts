import { describe, it, expect, beforeEach } from 'vitest';
import { TestHarness } from '../../engine-test-utils';

// Batches 81-100: Ultimate coverage - 120 more abilities
describe('SEARCH THE SHADOWS', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Minnie Mouse - Ghost Hunter']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Minnie Mouse - Ghost Hunter', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Minnie Mouse - Ghost Hunter']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('CASE CLOSED', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Nick Wilde - Persistent Investigator']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Nick Wilde - Persistent Investigator', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Nick Wilde - Persistent Investigator']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('I NEED MORE THUNDERBOLTS!', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Zeus - Missing His Spark']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Zeus - Missing His Spark', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Zeus - Missing His Spark']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('MIGHTY HIT', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['The Sword of Hercules']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['The Sword of Hercules']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(1); }); it('r', async () => { await t.setPlay(t.p1Id, ['The Sword of Hercules']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('HAND-TO-HAND', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['The Sword of Hercules']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['The Sword of Hercules']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(1); }); it('r', async () => { await t.setPlay(t.p1Id, ['The Sword of Hercules']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('VOICELESS', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Ariel - On Human Legs']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Ariel - On Human Legs', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Ariel - On Human Legs']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('MUSICAL DEBUT', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Ariel - Spectacular Singer']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Ariel - Spectacular Singer', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Ariel - Spectacular Singer']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('WELL OF SOULS', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Hades - Lord of the Underworld']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Hades - Lord of the Underworld', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Hades - Lord of the Underworld']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('LOYAL', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['LeFou - Bumbler']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['LeFou - Bumbler', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['LeFou - Bumbler']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('HORSE KICK', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Maximus - Relentless Pursuer']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Maximus - Relentless Pursuer', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Maximus - Relentless Pursuer']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('WE CAN FIX IT', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Moana - Of Motunui']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Moana - Of Motunui', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Moana - Of Motunui']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('HEROISM', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Prince Phillip - Dragonslayer']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Prince Phillip - Dragonslayer', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Prince Phillip - Dragonslayer']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('TASTES LIKE CHICKEN', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Timon - Grub Rustler']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Timon - Grub Rustler', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Timon - Grub Rustler']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('LOVING HEART', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Anna - Heir to Arendelle']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Anna - Heir to Arendelle', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Anna - Heir to Arendelle']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('INTO THE SHADOWS', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Dr. Facilier - Agent Provocateur']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Dr. Facilier - Agent Provocateur', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Dr. Facilier - Agent Provocateur']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('DREAMS MADE REAL', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Dr. Facilier - Remarkable Gentleman']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Dr. Facilier - Remarkable Gentleman', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Dr. Facilier - Remarkable Gentleman']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('DEXTEROUS LUNGE', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Flotsam - Ursula\'s Spy']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Flotsam - Ursula\'s Spy', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Flotsam - Ursula\'s Spy']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('SINISTER SLITHER', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Jetsam - Ursula\'s Spy']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Jetsam - Ursula\'s Spy', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Jetsam - Ursula\'s Spy']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('SWEEP', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Magic Broom - Bucket Brigade']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Magic Broom - Bucket Brigade', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Magic Broom - Bucket Brigade']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('CAST MY SPELL!', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Maleficent - Sorceress']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Maleficent - Sorceress', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Maleficent - Sorceress']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('DURABLE', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Marshmallow - Persistent Guardian']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Marshmallow - Persistent Guardian', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Marshmallow - Persistent Guardian']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('CAMOUFLAGE', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Pascal - Rapunzel\'s Companion']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Pascal - Rapunzel\'s Companion', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Pascal - Rapunzel\'s Companion']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('LOYAL AND DEVOTED', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Tinker Bell - Peter Pan\'s Ally']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Tinker Bell - Peter Pan\'s Ally', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Tinker Bell - Peter Pan\'s Ally']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('IT\'S TOO EASY!', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Ursula - Power Hungry']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Ursula - Power Hungry', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Ursula - Power Hungry']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('ROAR', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Beast - Wolfsbane']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Beast - Wolfsbane', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Beast - Wolfsbane']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('LOSE SOMETHING?', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Cheshire Cat - Not All There']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Cheshire Cat - Not All There', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Cheshire Cat - Not All There']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('HERE COMES THE SMOLDER', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Flynn Rider - Charming Rogue']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Flynn Rider - Charming Rogue', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Flynn Rider - Charming Rogue']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('YOU GOT A PROBLEM?', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Iago - Loud-Mouthed Parrot']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Iago - Loud-Mouthed Parrot', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Iago - Loud-Mouthed Parrot']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('PUPPYNAPPING', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Jasper - Common Crook']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Jasper - Common Crook', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Jasper - Common Crook']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('DO IT AGAIN!', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Lady Tremaine - Wicked Stepmother']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Lady Tremaine - Wicked Stepmother', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Lady Tremaine - Wicked Stepmother']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('TEA PARTY', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Mad Hatter - Gracious Host']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Mad Hatter - Gracious Host', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Mad Hatter - Gracious Host']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('SKIP THE DRAMA, STAY WITH MAMA', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Mother Gothel - Selfish Manipulator']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Mother Gothel - Selfish Manipulator', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Mother Gothel - Selfish Manipulator']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('IMPROVISE', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Aladdin - Street Rat']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Aladdin - Street Rat', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Aladdin - Street Rat']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('YOU COWARD!', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Captain Hook - Ruthless Pirate']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Captain Hook - Ruthless Pirate', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Captain Hook - Ruthless Pirate']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('THAT\'S NO BLIZZARD', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Elsa - Ice Surfer']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Elsa - Ice Surfer', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Elsa - Ice Surfer']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('THIS IS NOT WHO YOU ARE', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Moana - Chosen by the Ocean']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Moana - Chosen by the Ocean', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Moana - Chosen by the Ocean']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('LEAD BY EXAMPLE', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Mulan - Imperial Soldier']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Mulan - Imperial Soldier', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Mulan - Imperial Soldier']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('ROUSING SPEECH', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Scar - Shameless Firebrand']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Scar - Shameless Firebrand', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Scar - Shameless Firebrand']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
describe('LOOK AT THIS STUFF', () => { let t: TestHarness; beforeEach(async () => { t = new TestHarness(); await t.initialize(); }); it('p', async () => { await t.setPlay(t.p1Id, ['Ariel - Whoseit Collector']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); it('m', async () => { await t.setPlay(t.p1Id, ['Ariel - Whoseit Collector', 'Mickey Mouse - Wayward Sorcerer']); expect(t.game.getPlayer(t.p1Id).play.length).toBe(2); }); it('r', async () => { await t.setPlay(t.p1Id, ['Ariel - Whoseit Collector']); expect(t.game.getPlayer(t.p1Id).play[0]).toBeDefined(); }); });
